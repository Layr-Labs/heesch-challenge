"""Yukon evaluator for the Heesch challenge.

Runs as `PYTHONHASHSEED=0 python -P -m harness.verify` from the repo root
(the -P flag prevents anything under the competitor-editable submission/
directory from shadowing installed packages).
"""

__version__ = "1.0.0.dev0"
